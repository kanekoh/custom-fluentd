<store>
  @type forward

  self_hostname ${hostname}
  shared_key abcde

  secure yes

  ca_cert_path /etc/ocp-forward/ca_cert
  ca_private_key_path /etc/ocp-forward/ca_key
  ca_private_key_passphrase password

  <server>
    host https://fluentd.test01-hkaneko.svc.cluster.local:24224
  </server>
</store>

